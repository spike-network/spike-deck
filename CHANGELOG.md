# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 规范。

---

## [Unreleased]

### 🌐 多语言界面 (Localization)

- **中文与英文**：Popup、Options、运行工具、动态状态和扩展元数据支持中文与英文界面。
- **浏览器语言初始化**：首次安装根据浏览器首选语言选择中文或英文，并把结果保存为显式偏好。
- **即时切换**：设置页新增界面语言选项，切换后立即更新当前页面，并同步到其他已打开的 Deck 页面。

---

## [1.2.0] - 2026-08-29

### ⌨️ 快捷操作与无障碍 (Keyboard & Accessibility)

- **Popup 快捷键**：新增 `Command+Shift+S`（macOS）/ `Ctrl+Shift+S`（其他平台）呼出快捷键，可在设置中启用或禁用，并显示浏览器实际绑定、未绑定或冲突状态。
- **完整键盘选路**：支持 `/` 聚焦筛选、`Esc` 分层退出、方向键导航、`Home` / `End` 跳转及左右键展开或收起策略组。
- **焦点与窄屏体验**：面板打开后自动进入操作区域，关闭后返回触发按钮；补充键盘焦点、窄屏布局和 `prefers-reduced-motion` 支持。

### 🧭 状态连续性与操作反馈 (Continuity & Feedback)

- **渐进式加载**：Popup 优先渲染最近的有界策略组快照，再用实时数据替换；Core 暂时离线时明确显示缓存状态。
- **稳定加载布局**：首次显示前完成视口高度计算，并固定 Popup 的滚动区域与滚动条宽度，避免加载过程中出现整体或横向抖动。
- **任务集中反馈**：统一显示策略组测速、资源刷新和模块更新状态，支持从任务中心批量取消运行中的策略组测速。
- **选路反馈与撤销**：节点切换后提示仅影响新连接，可直接查看连接，并在短时间内撤销最近一次选择。
- **模式安全提示**：全部直连或全局代理模式下持续显示提示，并可一键恢复规则模式。
- **搜索与会话恢复**：搜索结果高亮并显示匹配数量；重新打开 Popup 时恢复面板、滚动位置和焦点，但不恢复筛选词。
- **可读性与辅助反馈**：溢出内容提供完整提示，监听地址、资源来源与错误信息支持点击复制，并通过独立 `aria-live` 区域播报操作结果。

---

## [1.1.1] - 2026-08-29

### ⏹ 取消组测速 (Cancel Group Tests)

- **运行中可取消**：策略组测速进行中再次点击 ⚡，通过 `DELETE /spike/group-tests/{id}` 取消该组全部测速任务。
- **按钮状态**：测速中显示「取消」，取消过程中显示「取消中」并禁用，避免重复提交。
- **后台同步**：Service Worker 取消后立即刷新任务快照；关闭 Popup 再打开仍能看到已取消的进度。

---

## [1.1.0] - 2026-08-29

### 🧰 运行工具工作区 (Runtime Workspace)

- **独立工具页**：Popup 顶栏新增扳手入口，打开独立页面，不受 Popup 生命周期限制。
- **连接**：查看 live / recent 连接，并可终止 live 连接。
- **DNS 与路由**：查看 DNS cache，执行 query / flush，以及对指定 host / port / protocol 做规则解释。
- **脚本**：列出已配置脚本并 Evaluate / Run cron，也可执行带 mock type 的临时脚本文本。

---

## [1.0.8] - 2026-08-26

### 🎛 交互与视觉精修 (Grouped List UI)

- **Surge / iOS 分组列表**：去掉紫青仪表盘配色，改为夜色表面 + 系统蓝选中行，延迟数字使用等宽数字。
- **流量方向**：状态条下行保持绿色、上行改为琥珀色，与工具栏角标方向识别一致。
- **交互细节**：焦点环、输入框光晕、行按压缩、尊重 `prefers-reduced-motion`；空状态与「打开控制台」文案改为可执行说明。
- **功能不变**：选路、测速、代理托管、隐藏组三态等逻辑未改。

---

## [1.0.7] - 2026-08-26

### 👁 隐藏策略组智能展示 (Smart Hidden Groups)

- **三态展示**：将「显示隐藏组」开关改为隐藏 / 智能 / 全部展示。
- **智能模式**：`hidden: true` 的组默认仍不出现；若它是当前可见组的选中成员（`override_member` 或 `selected`），则作为独立卡片列出，并沿着选中链路继续列出。
- **入口**：设置页下拉框与 Popup 策略组工具栏按钮循环切换；旧的布尔开关会迁移为「隐藏」或「全部展示」。

---

## [1.0.6] - 2026-08-26

### ⚡ 单节点测速状态修复 (Group Test Target)

- **保留测速目标节点**：刷新 Core 测速任务状态时，若快照未带回 `member` 字段，继续沿用启动时记录的目标节点，避免单节点测速被误当成整组测速。

---

## [1.0.5] - 2026-08-24

### 🔁 智能流量角标方向识别 (Direction-Aware Traffic Badge)

- **上传占优自动切换**：当实时上传速率超过下载速率时，工具栏图标角标改显上传速率，并以琥珀色（`#D97706`）区分；下载占优时维持青色显示下载速率。
- **一眼辨识流量方向**：无需展开面板即可判断当前是拉取为主还是推送为主（如上传备份、直播推流场景），悬停仍显示完整 `↓/↑` 双向统计。
- **零流量回退**：上下行均为 0 时保持原有行为——代理托管模式显示 `ON` 徽标，否则隐藏角标。

---

## [1.0.4] - 2026-08-24

### 🧭 零实例引导与空状态体验 (Zero-Instance Onboarding)

- **移除内置默认实例**：不再预置「Local Spike」默认实例，首次安装以干净的零实例状态启动，避免误导性配置。
- **Popup 欢迎引导页**：未配置实例时展示欢迎卡片，一键跳转设置页添加 Spike 实例；实例选择器显示「无可用实例」占位。
- **全面板空状态保护**：策略组、Providers、模块面板与 Profile 导出在无激活实例时显示友好提示，代理托管开关与内嵌面板操作自动拦截并提示先完成配置。
- **存储层健壮性**：允许删除全部实例，删除激活实例时自动回退到剩余实例或清空激活态，杜绝悬空的 `activeInstanceId`。

---

## [1.0.3] - 2026-08-24

### 🔒 安全与权限优化 (Security & Permissions)

- **权限最小化重构 (Least Privilege)**：移除 `manifest.json` 中全网通配的 `http://*/*` 与 `https://*/*` 静态主机权限，消除扩展安装时 Chrome 弹出的“读取和更改您在所有网站上的数据”高危权限警告。
- **本地白名单预授权**：默认 `host_permissions` 仅保留 `http://127.0.0.1/*` 与 `http://localhost/*`，本地 Spike 实例完全开箱即用、零弹窗打扰。
- **动态按需权限申请**：将通配规则放入 `optional_host_permissions`，新增 `lib/permissions.js` 模块：
  - 在 Options 页面测试连接、保存实例、设为激活时，若为局域网 IP（如 `192.168.x.x`）或远程域名实例，按需向用户动态申请目标主机权限。
  - 在 Popup 面板切换实例时提供权限检测与未授权拦截回退保护。
- **自动化测试**：新增 `tests/permissions.mjs` 覆盖主机模式提取、本地回环识别与 Mock 动态权限请求。

---

## [1.0.2] - 2026-08-24

### ⚡ 性能与高频探测 (Performance & Polling)

- **离屏文档 (Offscreen Document) 双轨调度**：引入 `offscreen.html` / `offscreen.js` 与 Service Worker 协同工作，突破 MV3 后台定时器受限瓶颈。
- **可配置刷新频率**：
  - 选项页支持自定义健康检查间隔（默认 5s）与流量速率刷新间隔（默认 1s）。
  - 窗口聚焦且有前台监控时启用 1s 极速速率刷新，窗口失焦/关闭时自动降频节能。
- **离屏生命周期管理**：自动按需创建与关闭离屏文档，保障后台长效稳定与内存占用最小化。

---

## [1.0.1] - 2026-08-24

### 🛡️ 容灾与商店发布工具链 (Resilience & Tooling)

- **代理健康容灾联动**：当后台健康检查发现 Spike Core 离线或不可达时，自动临时释放 Chrome 代理控制权交回系统/其他扩展，防止浏览器断网卡死；待 Spike 恢复后自动重新接管。
- **商店发布自动化**：新增 `scripts/cws.sh` 与 `Makefile` 自动化发布目标（`make store-upload`、`make store-submit`、`make store-release`），接入 Chrome Web Store API v2。
- **商店素材与截屏工具**：新增 `store/capture.sh` 及截图夹具页面，支持一键捕获 1280x800 高清商店推广图。
- **UI 紧凑化与视口约束**：优化 Popup 面板在小屏幕与不同分辨率下的展示，约束弹出层高度不超过可视标签页的 85%。

---

## [1.0.0] - 2026-08-24

### 🎉 初始版本发布 (Initial Release)

- **多实例集中管理**：支持多个 Spike Control API 实例的配置、测试、快速切换与持久化存储。
- **Surge 风格线路控制面板**：
  - 极简深色暗黑风格，实时展示 Profile 概览、策略组结构与节点列表。
  - 节点一键切换、固定策略组一键恢复自动选择。
  - 策略组与节点实时关键字搜索过滤。
- **延迟测速 (Speedtest / Ping)**：单组/单节点延迟测试，Service Worker 后台任务追踪，关闭 Popup 不中断测速任务。
- **浏览器代理托管模式**：一键将 Chrome 代理托管给 Spike，自动识别 `/spike/status` 中的 listeners（Mixed / HTTP / SOCKS5）。
- **外部资源管理**：支持 `policy-path`、`RULE-SET`、`DOMAIN-SET` 列表查看、单独更新与一键批量更新。
- **运行模式与配置管理**：支持规则模式 (Rule)、直连模式 (Direct)、全局模式 (Global) 切换；支持 Profile 切换与脱敏展示。
- **实时流量角标**：工具栏图标动态显示实时下行速率角标，Title 展示完整上传/下载流量统计。
